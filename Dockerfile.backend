FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
# Keep drizzle-kit available for runtime migration command.
RUN npm ci --include=dev

COPY . .

RUN chmod +x /app/docker/backend/start.sh

ENV NODE_ENV=production
ENV PORT=5000

EXPOSE 5000

CMD ["sh", "/app/docker/backend/start.sh"]
